{
    "tasks": {
        "coverage": "deno task test --clean --reporter=dot --coverage=coverage && deno coverage --lcov --output=lcov.info --exclude=\"vendor/.*\" coverage && genhtml -o coverage/report lcov.info",
        "test": "deno test -A --parallel"
    },
    "license": "MIT",
    "imports": {
        "@std/assert": "jsr:@std/assert@1"
    },
    "workspace": ["packages/*"],
    "fmt": {
        "indentWidth": 4,
        "singleQuote": true
    }
}
